import React, { Fragment } from 'react'
import HeaderLanding from '../HeaderLanding';

const index = (props) => {
    return (
        <Fragment>
            <HeaderLanding />
        </Fragment>
    );
};

export default index;