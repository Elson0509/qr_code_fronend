import React from 'react'
import api from '../../services/api'

const Reset = props => {
    console.log(props)
    return (
        <div>
            teste
        </div>
    );
};

export default Reset;